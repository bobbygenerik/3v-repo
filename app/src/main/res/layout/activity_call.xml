<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <org.webrtc.SurfaceViewRenderer
        android:id="@+id/remoteView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Top info removed; Compose overlay renders call info -->

    <!-- Rounded PIP with true clipping at top-right -->
    <FrameLayout
        android:id="@+id/pipFrame"
        android:layout_width="120dp"
        android:layout_height="234dp"
        android:layout_gravity="top|end"
        android:layout_margin="16dp"
        android:background="@drawable/bg_pip_rounded"
        android:clipToOutline="true"
        android:elevation="8dp">
        <com.example.threevchat.webrtc.ui.LocalTextureRenderer
            android:id="@+id/localView"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />
    </FrameLayout>

    <!-- Lightweight debug overlay for connection/ICE status -->
    <TextView
        android:id="@+id/debugOverlay"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|start"
        android:layout_margin="16dp"
        android:padding="8dp"
        android:background="#66000000"
        android:textColor="#FFFFFF"
        android:textSize="12sp"
        android:visibility="gone"
        android:typeface="monospace"
        android:maxLines="10" />

    <!-- Bottom controls removed; Compose overlay renders controls -->

    <!-- Edge buttons removed; Compose overlay renders actions -->

	<!-- Compose overlay for in-call controls and header (Compose-based UI) -->
	<androidx.compose.ui.platform.ComposeView
		android:id="@+id/composeOverlay"
		android:layout_width="match_parent"
		android:layout_height="match_parent"
		android:layout_gravity="fill"
		android:visibility="gone" />


</FrameLayout>
