// NativeStatsBridge removed per developer preference (no placeholders).
// If native getStats() is needed in future, implement platform handlers
// and re-add a well-tested bridge. For now, do not rely on native fallbacks.

// File intentionally left minimal to avoid accidental usage.
